services:
  - type: web
    name: exodus-daily
    runtime: docker
    region: oregon
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    disk:
      name: exodus-db
      mountPath: /data
      sizeGB: 1
